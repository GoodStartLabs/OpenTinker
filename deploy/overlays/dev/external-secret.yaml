apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: app-secrets
  namespace: opentinker-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: SecretStore
  target:
    name: app-secrets
    creationPolicy: Owner
  data:
  - secretKey: API_KEY
    remoteRef:
      key: opentinker-api-key-dev
  - secretKey: RABBITMQ_PASSWORD
    remoteRef:
      key: opentinker-rabbitmq-password-dev
  - secretKey: GCP_SERVICE_ACCOUNT_KEY
    remoteRef:
      key: opentinker-gcp-sa-key-dev
---
apiVersion: v1
kind: Secret
metadata:
  name: keda-rabbitmq-secret
  namespace: opentinker-dev
type: Opaque
stringData:
  host: amqp://admin:admin@rabbitmq.opentinker-dev.svc.cluster.local:5672//
