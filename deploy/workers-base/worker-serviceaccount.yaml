apiVersion: v1
kind: ServiceAccount
metadata:
  name: worker
  namespace: opentinker
  labels:
    app: opentinker-worker
  annotations:
    # Workload Identity annotation (will be patched in overlays)
    # iam.gke.io/gcp-service-account: opentinker-worker@PROJECT_ID.iam.gserviceaccount.com
